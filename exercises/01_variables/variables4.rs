// TODO: Fix the compiler error.
fn main() {
    // SOLUTION: ajouter le mot clé "mut" pour rendre la variable mutable :: c'est-à-dire qu'on peut changer sa valeur
    let mut x = 3;
    println!("Number {x}");

    x = 5; // Don't change this line
    println!("Number {x}");
}
